on: [pull_request]

jobs:
  golangci-lint:
    run-son: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: golangci-lint
        uses: reviewdog/action-golangci-lint@v2
        with:
          go_version: 1.20.4
